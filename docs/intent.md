This code depository will have a workshop which NOV can use to learn more about the AWS IOT stack in the workshop. There will be a AWS Claude development kit deployment, which will launch 3 IOT devices (small ec2 instances in a vpc) per workshop participant so make sure we launch the same stack 10 times that there are no conflicts. Then the users will update a bash script which AWS IOT device client uses to perform some update on the device, then we will run through a device shadow piece and then we will generate a failure and see how you can identify that. Let's try and make sure we don't violate the default max number of vpcs per account. Maybe we need to have a platform stack launch first that creates a VPC and use subnet and routing tables in order to isolate different sub nuts and let that be how we organize network isolation among the different sets of IOT devices deployed.


The workshop flow will include an initial CDK deployment that will check for existing VPCs named workshop-edge and workshop-cloud and if not create them, and then create a subnet in the workshop-edge VPC with network isolation so that the subnet can only reach a gateway and not other subnets in the VPC then three EC2 instances will be deployed into that subnet and their user data will install the AWS IOT device client there will be a fleet provisioning step that will use the provision by certain method where I figure in the user data we can include a CERT that the devices can use to register themselves, so the CDK deployment will also need to feature some logic that produces the CERT which we can later. Add to the EC2 instances probably via their user data. The EC2 instances should publish OS metrics to IOT core as soon as they registered, and the topic they published to should include an ID of the deployment, which is being used for the CDK deployment. A unique identifier should be generated, and that should be part of the topic the device is published to in AWS IOT core. The CDK deployment will also include an MSK cluster and a k8s cluster which use the workshop-cloud VPC.  The auto created MSK cluster should subscribe to all topics which use the deployment ID in the MQTT topic path and offload them to an iceberg table in S3 so the CDK deployment should also feature deploying an S3 bucket. In that way, after the deployment succeeds, the user should be able to navigate to S3 and see the files which are being created which initially will only have the operating system metrics from those EC2 instances. We will use Amazon Athena to query the open table format iceberg table, which has been created by Kafka so be sure to include a work group that the users can assume in order to make those Athena calls.

The first step in the workshop will be to look at some view of the current initially deployed set up. Our users will start in the IOT core AWS console and they will create a subscription to the relevant topic pattern and then they'll view the messages coming in from the edge devices. They will also go to S3 and look at the files that have been created and they will go to Amazon, Athena and query the data like they will be some kind of query which shows them data freshness so we need to have some pattern where the easy two instances have a clock synced with something that Athena can also sync with and in that way, determine the time between when the EC two incense measured the OS metric, and when the query returned that same metric in order to say how fresh the data is, which is being shown via that Athena query

The second step in the workshop will be to use the device client to make an update on the edge devices. The user will create an AWS IOT job that will add a shell script to the EC two instance and then execute shell script. It should in someway affect the data being sent over to IOT core. Let's have it both update the frequency at which data I sent so let's say first initially having the data come in at 0.2 Hz and then update it so that everything comes in at one hertz and also leave out one relevant OS metric from the initial deployment and in the second step will add that in so that we can both show the data frequency is changing and now there's this new piece of data coming into IT core based off o update. Let's also try and show how fleet management works in this context. Ideally, we have something where the users impressed by the fleet management capabilities of using IOT core IT jobs in this way.

In the third step well start using device shadows so probably initially I'd like there to be a name shadow per device that describes the configuration state of the device and the IT job should be something that update it and reports on say the current version of software deployed on the node. In this third step, we will add another name shadow and it will raise some other piece of state off that IOT device to make it also available in the cloud let's have the initial deployment only have the device config shadow and in this third step will add in the after deployment and device health shadows

| Shadow Name | Owned By | Contains |
|-------------|----------|----------|
| `app-deployment` | Cloud (desired) + Device (reported) | Compose version, container image tags, deploy status |
| `device-config` | Cloud (desired) + Device (reported) | Config version, telemetry interval, feature flags |
| `device-health` | Device (reported only) | CPU/memory/disk %, container count, uptime, last heartbeat |


In the fourth step of the workshop will add a UI on top of the system to make it kind of manageable. So here I plan to use AWS amplify in order to create a full stack application, which will allow us to interact with this IOT workload I think this actually makes sense to use for the whole dedicate deployment. The Kate deployment should be at Bass and AWS amplified deployment that we add custom components into in order to deploy the relevant CDK code mentioned above only in this fourth step will we start to use it here user should first off load a UI which will give them some visualization of this whole process so I think of some good open source way to visualize what's going on here and let's create ab sync resolvers in order to allow the front to get information about the system and then display it and some easy to use clear way to the there's probably also a step where we'll have to create a user in the Cognito user so maybe the first part of this fourth step is to run a script that allows you to create a user and then the user will load the website sign in and be able to see the relevant details. I'd like to have a feature in this fourth step where the user selects tags meaning timeseries measurements from the easy two instances to send to IOT car so there should be part of the device status the configured tags to send to Rotc and this front end should be able to update that shadow in order to change the metrics that are sent to IOT Core.

In the fifth step, will simulate a failure, so we'll have the user stop one of the EC2 instances and in the UI will see that reflected because the UI will read the device shadow and will be able to tell that there's been a failure either by the heartbeat being not fresh or by the device actually reporting a failure

In the sixth step will tackle the telemetry plane in the cloud. Here I would like to deployey timescaleDB, and rising wave in the cloud kubernetes cluster, and subscribe them to MSK. In the rising wave cluster, let's have a few default materialized views. To start with let's do something around device resource consumption so let's do a time series of say the last five minutes of CPU usage and aggregation of total memory used across all edge devices. There should be an appsync resolver, which will be able to interact with the rising wave cluster in order to bring that data into the front end and display it. I figured there will need to be some kind of subscription which pulls live from the rising wave cluster. The front end should also have a query interface that connects to that time scale DB and allows you to run queries against it but through the graph QAPI so we need a resolver that will like to execute queries against that time scaleDB. I'd like to have a subscription also against the timescale DB to show how the query freshness is different between query directly from rising wave and running the query against time scale DB and displaying it so use whatever pattern is common for live dashboard with time scale, DB and run it through ab sync and in the front end will display a plot there too to compare the data freshness between the two storage options. Also include a plot which queries directly from the iceberg table in S3 to show similar data so at the end, I'll get an apple apple comparison of the three methods of serving data from data stores to show how their performance characteristics differ.

In the seventh step, I'd like to tackle the telemetry plane at the edge. First will deploy the edge stack from the real-time pipeline architecture file (docs/notes/real-time-pipeline-architecture.md) by using an IOT job so in this job will deploy K3S for Kubernetes across our three edge nodes, and on the edge cluster will deploy all of the edge components in that real-time pipeline architecture document. Well, also simulate all the sensors you might find at a normal well fracture in site. They will publish data to that MQTT broker in the edge stack. I'd like to have an observation layer here too so in that edge stack in the Kunze cluster there needs to be some kind of HMI, which has some visualization that looks like a fright, but also lets you mouse over and see data coming from the various pieces of equipment. The users of the workshop will use port forwarding in order to forward a port to their local laptop that loads this HMI so they can see what that can look like. The HMI needs to have a page for digital operation related metrics like how big is the queue of data that has not yet been uploaded to the cloud. Ideally, that'll be zero when operations are running well, but will also similar to a failure of network and the users will see how the data starts to build up at the edge in a safe way. The HMI will be a react app which uses next JS as its framework. 


## Links
- Relevant Data Pipeline information: /Users/waltmayf/Library/CloudStorage/OneDrive-SharedLibraries-amazon.com/Energy TFC - Digital Operations - Documents/5000 - Ideas & Backlog/5100 - Idea Submissions/2026-05-01-open-data-pipeline