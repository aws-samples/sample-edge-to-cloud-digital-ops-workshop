tmp
.claude
**/.next
META-INF
cdk.context.json
.vscode

# Helm chart tarballs — run `helm dependency update helm/<chart>` to regenerate
helm/edge-stack/charts/*.tgz
helm/cloud-analytics/charts/*.tgz
node_modules
.DS_Store
.amplify
amplify_outputs.json
# CLAUDE.md is committed (public guidance). Internal/machine-local notes live in
# CLAUDE.private.md, which it imports and which stays local-only.
CLAUDE.private.md
cdk.out**
*.js.map

workshop-site.zip

# Generated deployment artifact (contains account-specific ARNs)
DEPLOYMENT_SUMMARY.md

# E2E test reports
e2e/reports/

# Local test scripts (not for distribution)
job-scripts/telemetry-v4.sh

# #257: telemetry-v2.sh is the Session 2 IoT Job exercise deliverable —
# block-2-iot-job.md has participants generate it from telemetry-v1.sh, so it
# must not be a pre-existing committed file (that would spoil the exercise and
# the before/after precision demo).
job-scripts/telemetry-v2.sh

# Python / MkDocs
.venv
__pycache__
*.py[cod]
*.pyo
.Python
site/

# Legacy pre-migration build artifacts (replaced by Firehose → Iceberg; ~2GB of
# local JARs/Maven output, never committed). See #124 for the migration.
flink-hudi-local/
flink-hudi-sink/
local-kafka-connect/

# Java / Maven build output
target/
*.jar
*.class

# HMI / container build artifacts
hmi/*.tar.gz
workshop-hmi.tar.gz
*.tar.gz

# Editor / OS cruft
*.swp
*~
.idea/

