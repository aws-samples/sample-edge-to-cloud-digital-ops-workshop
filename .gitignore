tmp
.claude
**/.next
META-INF
cdk.context.json
.vscode

# Helm chart tarballs — run `helm dependency update helm/edge-stack` to regenerate
helm/edge-stack/charts/*.tgz
node_modules
.DS_Store
.amplify
amplify_outputs.json
CLAUDE.md
cdk.out**
*.js.map

workshop-site.zip

# Maven build output
**/target/

# Vendored Kafka Connect plugin JARs (too large for git; rebuild with build-plugin.sh)
local-kafka-connect/plugins/
local-kafka-connect/.jar-cache/
local-kafka-connect/hudi-kafka-connect-bundle-0.14.1.jar

# Generated deployment artifact (contains account-specific ARNs)
DEPLOYMENT_SUMMARY.md

# E2E test reports
e2e/reports/

# Local test scripts (not for distribution)
job-scripts/telemetry-v4.sh

# Python / MkDocs
.venv
__pycache__
*.py[cod]
*.pyo
.Python
site/


